{% extends 'base.html.twig' %}

{% block title %}Hello{% endblock %}

{% block body %}
    <h2 style="text-align: center; margin-bottom: 30px;" id="titleWods">Tous mes wods</h2>

    {% if listWods is empty %}
        <div class="container" style="text-align:center; font-size: 18px;">
                <p>Vous n'avez pas encore enregistré d'entrainements, commencez de suite en <a href="{{ path('wod') }}" style="text-decoration: none; color: grey;" onmouseover="this.style.color='black'; this.style.textDecoration='underline';" onmouseout="this.style.color='grey'; this.style.textDecoration='none';">partagant votre dernier entrainement avec la communauté </a>!
        </div>

    {% endif %}
    <div class="container" style="text-align:center;">
        <select id="selectList" class="form-control">
            <option>Tous mes wods</option>
            <option>Wods effectués</option>
            <option>A faire plus tard</option>
        </select>
        <div id="list">
        </div>
        <div  id="userId" style="display: none;">{{ listWods[0].userId }}
        </div>
        <div id="wods">
    {% for wod in listWods %}
         <div class="container">
                <div class="card">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">
                            <h2 class="text-light display-6">  {{ wod.name|upper }}</h2>
                                <p>{{ wod.date }}</p>
                        </li>
                        {% if wod.time is empty %}
                        {% else %}
                           <li class="list-group-item">
                            <p class="title-list-group-item">Temps:</p> 
                            <div class="content-list-group-item">{{ wod.time }}</div> </li>
                        {% endif %}
                        <li class="list-group-item">
                        <p class="title-list-group-item">Workout:</p><div class="content-list-group-item">
                            {{ wod.work|raw }}</div>
                        </li>
                        {% if wod.weight is empty %}
                        {% else %}
                             <li class="list-group-item">
                        <p class="title-list-group-item">Poids utilisés:</p><div class="content-list-group-item">
                            {{ wod.weight|raw }}</div>
                        </li>
                        {% endif %}
                       
                        {% if wod.comment is empty %}
                        {% else %}
                            <li class="list-group-item">
                        <p class="title-list-group-item">Commentaire:</p><div class="content-list-group-item">
                            {{ wod.comment|raw }}</div>
                        </li>
                        {% endif %}
                        

                    </ul>

            </div>
        </div>


    {% endfor %}
</div>
<div id="wodsTodo">
     {% for wodTodo in listWodsTodo %}
         <div class="container">
                <div class="card todo">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">
                            <h2 class="text-light display-6">  {{ wodTodo.name|upper }}</h2>
                                <p>{{ wodTodo.date }}</p>
                        </li>
                        {% if wodTodo.time is empty %}
                        {% else %}
                           <li class="list-group-item">
                            <p class="title-list-group-item">Temps:</p> 
                            <p>{{ wodTodo.time }}</p>
                        </li>
                        {% endif %}
                        <li class="list-group-item">
                       <p class="title-list-group-item">Workout:</p>
                            <p>{{ wodTodo.work|raw }}</p>
                        </li>
                        {% if wodTodo.weight is empty %}
                        {% else %}
                             <li class="list-group-item">
                        <p class="title-list-group-item">Poids utilisés:</P><p>
                            {{ wodTodo.weight|raw }}</p>
                        </li>
                        {% endif %}
                       
                        {% if wodTodo.comment is empty %}
                        {% else %}
                            <li class="list-group-item">
                       <p class="title-list-group-item">Commentaire:</p><p>
                            {{ wodTodo.comment|raw }}</p>
                        </li>
                        {% endif %}
                        <li class="list-group-item">
                                <a href="{{ path('wod', {'name': wodTodo.name, 'workout': wodTodo.work, 'weights': wodTodo.weight}) }}"><button class="btn btn-success btn-lg">J'ai fait ce WOD</button></a>
                        </li>
                        

                    </ul>

            </div>
        </div>


    {% endfor %}
</div>
{% endblock %}
